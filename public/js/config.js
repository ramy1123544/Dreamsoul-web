// ══════════════════════════════════════
//   إعدادات الموقع — عدّل هنا فقط
// ══════════════════════════════════════

const CONFIG = {
  // رابط الـ API (البوت على Wispbyte)
  // مثال: 'http://1.2.3.4:3001'
  API_URL: 'http://93.115.101.140:15345',

  // كلمة السر (نفس API_KEY في .env)
  API_KEY: 'svhg08-25xze-36',

  // كم ثانية بين كل تحديث تلقائي
  REFRESH_INTERVAL: 30,
};
